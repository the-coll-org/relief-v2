node_modules/
.next/
dist/
*.local
.env
.DS_Store
prisma/dev.db*
test-results/
playwright-report/
.next/
data/live-entities.json
# Seed snapshot contains NGO focal-point contact PII — delivered out-of-band,
# never committed to this public repo (see infra-handoff.md "Privacy note").
data/backup-lbresponse-db.json
logs/

# Detached build worktrees created by scripts/deploy.sh (canonical clone only).
.deploy/

# Internal ops handoff — server IP + credential pointers; never in the public repo.
infra-handoff.md
